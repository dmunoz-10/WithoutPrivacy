$('#div_next_link').after(`<%= j render partial: 'messages/message', collection: @messages.reverse, as: :message %>`)

<% if @pagy.next.nil? %>
  $('#div_next_link').remove()
<% else %>
  $('#div_next_link').html(`<%== pagy_next_link(@pagy, '', 'id="next_link"') %>`)
<% end %>
